<div class="container">
<h2> <%= @post.title %> </h2>
<p> <%= @post.body %> </p>

<h3> Commentaire</h3>
<%= render @post.comments %>
<%= render 'comments/form' %>
<br>
<%= link_to "edit", edit_post_path(@post),  class: "btn btn-default btn-primary"%>
<%= link_to "Home", root_path,class: "btn btn-default btn-primary" %>
<%= link_to "suprimer", post_path(@post), method: :delete, data: { confirm: "tu es sur ?"} %>
</div>
